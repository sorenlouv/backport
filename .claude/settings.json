{
  "permissions": {
    "allow": [
      "Bash(npx *)",
      "Bash(npm *)",
      "Bash(gh *)",
      "Bash(node *)",
      "Bash(git add:*)",
      "Bash(git cherry-pick:*)",
      "Bash(git commit:*)"
    ]
  },
  "defaultMode": "bypassPermissions",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=$(git worktree list | head -1 | awk '{print $1}') && ([ -f .env ] || cp \"$root/.env\" .env 2>/dev/null || true) && ([ -d node_modules ] || cp -r \"$root/node_modules\" . 2>/dev/null || true)"
          }
        ]
      }
    ]
  }
}
